{
    "name": "regexp",
    "private": false,
    "license": "Apache-2.0",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vue-tsc && vite build",
        "preview": "vite preview",
        "deploy": "vue-tsc && vite build && git add . && git commit -m 'sync' && git push origin && cd dist && git add . && git commit -m 'deploy' && git push -f"
    },
    "dependencies": {
        "vue": "^3.3.4"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^4.2.3",
        "typescript": "^5.0.2",
        "vite": "^4.4.5",
        "vue-tsc": "^1.8.5"
    }
}
